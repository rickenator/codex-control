import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rickenator.consiglio.mobile',
  appName: 'Consiglio',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};

export default config;
